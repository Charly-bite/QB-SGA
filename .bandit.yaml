# Bandit config file
skips:
  - B101 # assert_used
  - B110 # try_except_pass
  - B311 # random
  - B404 # import_subprocess

# severity_level is typically passed via command line, but we can set thresholds if needed.
